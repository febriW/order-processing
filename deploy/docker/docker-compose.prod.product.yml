name: be-order-processing-product

services:
  product_service:
    build:
      context: ../..
      dockerfile: service/product/Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL}
      AUTH_VALIDATE_URL: ${AUTH_VALIDATE_URL}
    ports:
      - "${PRODUCT_PORT:-8082}:8082"
    restart: unless-stopped
